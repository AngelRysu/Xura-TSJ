declare module 'md5' {
    /**
     * Devuelve un hash MD5 del mensaje proporcionado.
     * @param message El string a convertir en hash.
     * @returns Hash MD5 como string hexadecimal.
     */
    // eslint-disable-next-line no-unused-vars
    export default function md5(message: string): string;
  }
